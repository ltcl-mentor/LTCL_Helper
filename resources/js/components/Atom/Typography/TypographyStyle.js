// コードブロックのスタイル
export const styleCodeblock = {
    width: "90%",
    marginLeft: "5%",
    marginTop: 1,
    marginBottom: 1,
    padding: 2,
    backgroundColor: "#DDDDDD",
    borderRadius: "3px"
};

// 参考記事登録のスタイル
export const styleArticleRegistration = {
    marginRight: "5%"
};

// 参考記事のスタイル
export const styleReferenceArticle = {
    marginTop: 3,
    borderBottom: "1px solid gray",
    fontWeight: "bold",
    padding: 1,
    marginX: "5%"
};

// PC用質問タイトルのスタイル
export const stylePcQuestionTitle = {
    width: "70%",
    marginTop: 4,
    fontSize: 30,
    fontWeight: "bold"
};

// PC用質問項目タイトルのスタイル
export const stylePcTitle = {
    marginTop: 3,
    borderBottom: "1px solid gray",
    fontWeight: "bold",
    padding: 1
};

// PC用質問詳細のスタイル
export const stylePcQuestionRemarks = {
    marginTop: 1,
    padding: 2,
    marginBottom: 2
};

//　モバイル用質問タイトルのスタイル
export const styleMobileQuestionTitle = {
    marginTop: 4,
    fontSize: 20,
    fontWeight: "bold"
};

// モバイル用小タイトルのスタイル
export const styleMobileTitle = {
    marginTop: 3,
    borderBottom: "1px solid gray",
    fontWeight: "bold",
    padding: 1,
    fontSize: 15
};

// モバイル用質問詳細のスタイル
export const styleMobileQuestionRemarks = {
    fontSize: 15,
    padding: 1,
    marginBottom: 2
};

// 公開非公開ボタンのスタイル
export const styleReleaseButton = { marginTop: 1, marginBottom: 1 };

// 質問記入用のスタイル
export const styleEditTitle = {
    marginTop: 4,
    marginBottom: 10,
    fontSize: 30,
    color: "#771af8",
    fontWeight: "bold"
};

// カテゴリータイトルのスタイル
export const styleCategoryTitle = {
    fontWeight: "bold",
    fontSize: 20
};

// 質問の編集画面サブタイトルのスタイル
export const styleEditSubTitle = {
    fontWeight: "bold",
    fontSize: 20,
    mt: 6,
    marginBottom: 2
};

// コメントのスタイル
export const styleComment = {
    paddingTop: 2,
    marginLeft: 4,
    marginBottom: 2,
    marginRight: 2,
    clear: "left"
};

// モバイル用コメントのスタイル
export const styleMobileComment = {
    m: 2,
    fontSize: 15
};

// 質問のレビュー用スタイル
export const styleReview = {
    width: "100%",
    minHeight: "350px",
    padding: "5%",
    wordWrap: "break-word"
};
