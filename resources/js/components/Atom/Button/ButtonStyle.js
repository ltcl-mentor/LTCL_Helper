// 編集と削除ボタンのスタイル
export const styleEditDelete = {
    color: "#771AF8",
    fontSize: 20
};

// モバイル用編集、削除ボタンのスタイル
export const styleMobileEditDelete = {
    color: "#771AF8",
    fontSize: 15
};

// 記事登録ボタンのスタイル
export const styleArticleRegistrationButton = {
    color: "#771AF8",
    textDecoration: "underline",
    marginBottom: "-5%",
    fontSize: 18,
    fontWeight: "bold"
};

// 質問一覧に戻るボタンのスタイル
export const styleReturnQuestionButton = {
    color: "#771AF8",
    textDecoration: "underline",
    marginBottom: "-5%",
    fontSize: 18,
    fontWeight: "bold"
};

// 公開確認用モーダルの閉じるボタン
export const styleCloseModalButton = {
    color: "red",
    marginLeft: "95%",
    marginBottom: "5%"
};

// 個別質問ページへ戻るためのボタン
export const styleReturnIndividualButton = {
    margin: "3%",
    borderBottom: "1px solid #771af8",
    color: "#771af8",
    borderRadius: 0
};

// 新規コメント投稿用ボタン
export const CommentButton = {
    variant: "outlined",
    color: "#771af8",
    border: "2px solid #771af8",
    fontWeight: "bold",
    minWidth: 150,
    maxWidth: 200,
    marginBottom: 5,
    fontSize: 15,
    "&:hover": {
        backgroundColor: "#771AF8",
        color: "white"
    }
};

// コメント送信用ボタン
export const PostButton = {
    variant: "outlined",
    color: "#771af8",
    border: "2px solid #771af8",
    fontWeight: "bold",
    minWidth: 100,
    maxWidth: 150,
    marginBottom: 5,
    fontSize: 15,
    "&:hover": {
        backgroundColor: "#771AF8",
        color: "white"
    }
};

// Topページに戻るためのボタン
export const styleReturnTopButton = {
    color: "black",
    minWidth: 150,
    maxWidth: 200,
    marginBottom: 5
};
