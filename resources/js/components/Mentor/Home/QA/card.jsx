import React from "react";
import { CardText, CardArea, StyleCard } from "@/Styles/Mentor/Home/QA/ForMentorContent";

/**
 * 対応待ちカード
 */
const card = () => {
    return (
        <CardArea>
            <StyleCard>
                <CardText>対応待ち</CardText>
            </StyleCard>
        </CardArea>
    );
};

export default card;
